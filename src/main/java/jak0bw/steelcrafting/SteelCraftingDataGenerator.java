package jak0bw.steelcrafting;

import net.fabricmc.fabric.api.datagen.v1.DataGeneratorEntrypoint;
import net.fabricmc.fabric.api.datagen.v1.FabricDataGenerator;

/**
 * Data generator entry point for the SteelCrafting mod.
 * Currently using manual JSON files instead of data generation.
 */
public class SteelCraftingDataGenerator implements DataGeneratorEntrypoint {
	@Override
	public void onInitializeDataGenerator(FabricDataGenerator fabricDataGenerator) {
		// No data generation needed - using manual JSON files
	}
}
